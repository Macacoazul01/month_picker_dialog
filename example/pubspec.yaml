name: month_picker_dialog_2_example
description: Demonstrates how to use the month_picker_dialog_2 plugin.
publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2  

dev_dependencies:
  flutter_test:
    sdk: flutter

  month_picker_dialog_2:
    path: ../

flutter:
  uses-material-design: true